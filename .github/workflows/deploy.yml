name: Deploy to EC2
on:
  push:
    branches: [master]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

      # Paso crítico: Configuración de SSH con ssh-agent
      - name: Add SSH Key
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.EC2_KEY }}

      # Verificación de conexión SSH (debug)
      - name: Test SSH Connection
        run: |
          ssh -o StrictHostKeyChecking=no -i ${{ secrets.EC2_KEY }} ubuntu@${{ secrets.EC2_HOST }} "echo '✅ Conexión SSH exitosa'"

      # Despliegue en EC2
      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_HOST }} << 'EOF'
            cd /home/ubuntu/userapp-backend
            echo "🔄 Deteniendo aplicación..."
            pm2 stop userapp-backend || true
            echo "📥 Actualizando código..."
            git pull origin master
            echo "🧹 Limpiando dependencias..."
            rm -rf node_modules
            echo "📦 Instalando dependencias..."
            npm ci
            echo "🏗️ Compilando aplicación..."
            npm run build
            echo "🚀 Iniciando aplicación..."
            pm2 start npm --name "userapp-backend" -- run start:prod
            echo "✅ Despliegue completado!"
          EOF
